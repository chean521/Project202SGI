<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user_pc.project202">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewAct"
            android:label="View Activity"></activity>
        <activity android:name=".ViewDetail"
            android:label="View Activity Details"></activity>
        <activity android:name=".ViewCand"
            android:label="View Candidate Result"></activity>
        <activity android:name=".AboutSystem"
            android:label="About System"></activity>
        <activity android:name=".StudentLogin"
            android:label="Student Login"> </activity>
        <activity android:name=".VoteList"
            android:label="Make Vote"></activity>
        <activity android:name=".PanelVote"
            android:label="Vote Panel Selection"></activity>
    </application>

</manifest>